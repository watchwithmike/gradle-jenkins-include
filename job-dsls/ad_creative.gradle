def projectBranches = ['master', 'develop']

projectBranches.each { projectBranch ->
    def jobName = "ad-creative ${projectBranch.toUpperCase()}"
    job(jobName) {
        logRotator(-1, 20, -1, -1)
        triggers {
            githubPush()
        }
    }
}
